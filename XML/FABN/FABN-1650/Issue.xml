<Issue id="46168" key="FABN-1650" number="1650" project="10604" reporter="davidkel" assignee="bestbeforetoday" creator="davidkel" type="10004" summary="gateway connect logs an error, but doesn&apos;t throw an error to say there was a problem with connection" priority="3" resolution="10000" status="6" created="2020-09-30 10:49:35.0" updated="2020-10-06 15:09:02.0" resolutiondate="2020-10-06 15:09:02.0" votes="0" watches="1" workflowId="60073" archived="N"> <description><! CDATA Is the not throwing an error intentional ? If so it's confusing to get an error log entry but the connect appears to work.  For example in networkconfig.ts the code here is   {code:java} async function buildPeer(client: Client, peerName: string, peerConfig: any, config: any): Promise<void> {     const method = 'buildPeer';     logger.debug('%s  start  %s', method, peerName);     const mspid = findPeerMspid(peerName, config);     const options = await buildOptions(peerConfig);     const endpoint = client.newEndpoint(options);     try  {         logger.debug('%s - about to connect to endorser %s url:%s mspid:%s', method, peerName, peerConfig.url, mspid);         // since this adds to the clients list, no need to save         const peer = client.getEndorser(peerName, mspid);         await peer.connect(endpoint);         logger.debug('%s - connected to endorser %s url:%s', method, peerName, peerConfig.url);     }  catch (error)  {         logger.error('%s - Unable to connect to the endorser %s due to %s', method, peerName, error);     } } {code}   And there are other areas where the same pattern can be observed for instance `buildOrderer` in the same file as well as similar functions in DiscoveryService.js  ></description> </Issue>
