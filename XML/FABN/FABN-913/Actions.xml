<Action id="63427" issue="33596" author="harrisob@us.ibm.com" type="comment" created="2019-08-30 10:47:04.0" updateauthor="harrisob@us.ibm.com" updated="2019-08-30 10:47:04.0"> <body><! CDATA with the new low level all connection options are centralized in an "Endpoint" object. This object originates from the "Client" object by combining the default settings from the SDK configuration and any unique settings for that endpoint.  const options = { url: grpcs://myhost:80, pem: 'PEM...' }; const endpoint = client.newEndpoint( options );  later the endpoint may be used to connect to all services at that endpoint  peer.connect(endpoint); discoverer.connect(endpoint); eventer.connect(endpoint);  ></body> </Action>
