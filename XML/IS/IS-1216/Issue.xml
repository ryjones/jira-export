<Issue id="38369" key="IS-1216" number="1216" project="10401" reporter="esplinr" creator="esplinr" type="10006" summary="Improve performance for applications tracking the ledger" priority="3" status="10405" created="2019-03-12 01:03:43.0" updated="2019-03-29 21:04:30.0" votes="0" watches="1" workflowId="50771"> <description><! CDATA *Story* As an administrator monitoring a deployment of the Indy Ledger, I want my application to be able to download all ledger transactions without unnecessarily loading the network or slowing down my application so that I can have multiple ledger browsers tailored to my specific search needs.  *Notes* * Currently a ledger browser uses GET_TXN for this, which is not optimized for this purpose. * GET_TXN is not a good way to download the ledger because it doesn't have state proofs / BLS multi-sig support yet (INDY-1954 in 1.9 release), so it's sent to all the Nodes and waits for f+1 equal replies (unlike other GET requests with state proof support). ￼* Even if GET_TXN had state proofs/BLS multi-sig, it still would be not so efficient for download-all use case. * One option is to add an SDK command to use already existent catch-up protocol for DOMAIN ledger (it uses it for POOL ledger only as of now)  ></description> </Issue>
