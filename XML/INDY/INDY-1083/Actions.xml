<Action id="38667" issue="26707" author="ashcherbakov" type="comment" created="2018-01-16 08:58:30.0" updateauthor="ashcherbakov" updated="2018-01-16 08:59:12.0"> <body><! CDATA Changes: - added dynamic validation for ClaimDef transaction to not allow update of a ClaimDef's data (public key) for the same (issuer-did; scheam-ref; signature-type). - moved validation for Schema from static to dynamic - added more tests - re-factored `test_public_repo` (split into two files; use parameters to avoid duplicate code)  PR: - https://github.com/hyperledger/indy-node/pull/522  Build - master 1.2.273  Tests: - `test_schema` - `test_claim_def` - `test_send_claim_def`  Risk: - Low  Recommendations for QA: - add ClaimDef txn - try to add ClaimDef by the same user for the same Schema (should not be possible) - try to add ClaimDef by a different user for the same Schema (should be possible) - try to add ClaimDef by the same user for a different Schema (should be possible)   ></body> </Action>
<Action id="38682" issue="26707" author="vladimirwork" type="comment" created="2018-01-16 14:46:55.0" updateauthor="vladimirwork" updated="2018-01-16 14:46:55.0"> <body><! CDATA Build Info: indy-node 1.2.273  Steps to Validate: 1. Send Schema as Trustee. 2. Add ClaimDef as Trustee for Step 1 Schema. 3. Try to add another one ClaimDef as Trustee for Step 1 Schema. 4. Send another Schema as Steward. 5. Add ClaimDef as Steward for Step 1 Schema. 6. Add ClaimDef as Trustee for Step 4 Schema.  Actual Results: All steps are passed except Step 3 (as expected).  ></body> </Action>
