<Action id="51738" issue="34420" author="toktar" type="comment" body="Fixed in PR  https://github.com/hyperledger/indy-node/pull/960 " created="2018-10-04 08:27:24.0" updateauthor="toktar" updated="2018-10-04 08:27:24.0"/>
<Action id="51740" issue="34420" author="toktar" type="comment" created="2018-10-04 10:38:27.0" updateauthor="toktar" updated="2018-10-04 10:38:27.0"> <body><! CDATA Problem reason: - Restarter class contains calls of unimplemmented methods.  Changes: - Removed sendMessageUponNodeRestartComplete() and sendMessageUponNodeRestartFail() calls in Restarter class.  PR: *  https://github.com/hyperledger/indy-node/pull/960   Version: * indy-node 1.6.622 -master * indy-plenum 1.6.553 -master  Risk factors: - No risk  Covered with tests: *  test_pool_restart.py|https://github.com/hyperledger/indy-node/pull/960/files#diff-5f88942e85e13268c921603c686868ac   Recommendations for QA: * Test restart command for a future and check that pool can ordered any transaction (NYM for example) after it.  ></body> </Action>
<Action id="51753" issue="34420" author="nataliadracheva" type="comment" created="2018-10-04 13:21:44.0" updateauthor="nataliadracheva" updated="2018-10-04 13:26:57.0"> <body><! CDATA *Scenario 1:* *Build version:*  indy-node: 1.6.622 indy-plenum: 1.6.554 *Test description:* Verify if pool keeps writing when scheduled restart happens. *Preconditions:* *Steps to Reproduce:* # Schedule a pool restart in near future: {code:java}  did new seed=000000000000000000000000Trustee1  did use V4SGRU86Z58d6TV7PBUe6f  ledger pool-restart action=start datetime=2018-09-21T14:44:00.000000+00:00 {code} # Once restart happens, send nym and get-nym by indy-cli.  *Expected results:* Pool writes the nym to the ledger and sends nym details when get-nym is requested. *Actual results:* Pool writes the nym to the ledger and sends nym details when get-nym is requested. (/)  *Scenario 2:* *Build version:*  indy-node: 1.6.622 indy-plenum: 1.6.554 *Test description:* Verify if pool keeps writing when scheduled restart happens. *Preconditions:* *Steps to Reproduce:* # Schedule a pool restart somewhen in future.  # Schedule a pool restart in near future # Once restart happens, send nym and get-nym by indy-cli.  *Expected results:* Pool writes the nym to the ledger and sends nym details when get-nym is requested. *Actual results:* Pool writes the nym to the ledger and sends nym details when get-nym is requested. (/)  *Scenario 3:* *Build version:*  indy-node: 1.6.622 indy-plenum: 1.6.554 *Test description:* Verify if pool keeps writing when scheduled restart cancelled. *Preconditions:* *Steps to Reproduce:* # Schedule a pool restart somewhen in future.  # Cancel the pool restart. # Send nym and get-nym by indy-cli.  *Expected results:* Pool writes the nym to the ledger and sends nym details when get-nym is requested. *Actual results:* Pool writes the nym to the ledger and sends nym details when get-nym is requested. (/)  ></body> </Action>
