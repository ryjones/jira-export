<Issue id="20993" key="INDY-873" number="873" project="10303" reporter="dsurnin" assignee="dsurnin" creator="dsurnin" type="10002" summary="Generalize config usage in code" priority="3" resolution="10001" status="10001" created="2017-09-26 09:08:28.0" updated="2019-03-29 20:35:47.0" resolutiondate="2019-03-29 20:35:47.0" votes="0" watches="2" workflowId="20998"> <description><! CDATA * For now config parameters such as base dir, data dir, log dir, etc. are defined differently in different classes: some of them are passed via constructor parameters, some are taken directly from config, some are calculated. As a result it is hard to reconfigure such parameters.  We need to remove direct config usage from the code. All the needed parameters should be read once from config and passed via constructors.     * Tests do not use config. Testing environment is configured via temporary directories which are passed as a parameters. As a result it is hard to add additional configuration parameter and modify existing ones.  We need to create special test config for the test environment.  ></description> </Issue>
