<Issue id="41806" key="INDY-2205" number="2205" project="10303" reporter="ashcherbakov" assignee="vladimirwork" creator="ashcherbakov" type="10001" summary="Automated load test analysis" priority="3" resolution="10000" status="10001" created="2019-08-15 09:15:31.0" updated="2019-09-13 16:07:33.0" resolutiondate="2019-09-13 12:18:24.0" votes="0" watches="2" workflowId="54845"> <description><! CDATA *Story* As a developer or QA analyzing load test results, I need to have all common errors and exception already grepped and reported, so that I can understand faster what's going on.  It would save a lot of time if load script automation could grep all common errors and exception in logs (each Node logs and journalctl).     *Acceptance criteria*  The result of load tests need to have the following information (as a report): * How many transactions have been written on every node * How many transaction expected to be written (from the load script point of view) * How many view changes happened * Graphs from each nodes * Metrics summary from each node (`get_metrics` output) * Get validator info result from each node * Journalctk analysis: ** output all exceptions (if any) * Logs analysis: ** All ERROR log messages ** The following patterns: *** `blacklisting` *** `invalid BLS signature` *** `request digest is incorrect` *** `has incorrect digest` *** `time not acceptable` *** `incorrect state trie root` *** `incorrect transaction tree root` *** `has incorrect reject` *** `error in plugin field` *** `incorrect audit ledger` *** <to be continued......>  ></description> </Issue>
