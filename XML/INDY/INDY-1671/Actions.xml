<Action id="52271" issue="33413" author="esplinr" type="comment" created="2018-10-15 14:52:47.0" updateauthor="esplinr" updated="2018-10-15 14:52:47.0"> <body><! CDATA When the node service fails due to OOM, it automatically restarts. This could recreate the same issue that caused it to fail previously.  A few ways we can improve the situation: * Notify the admin that we are failing due to OOM * Devise a way to change the system on the reload so as not to fail  ></body> </Action>
