<Action id="34657" issue="20777" author="vladimirwork" type="comment" created="2017-11-16 10:51:49.0" updateauthor="vladimirwork" updated="2017-11-16 10:54:59.0"> <body><! CDATA Build Info: indy-node 1.2.210 (master) libindy 1.1.0 (master)  Steps to Validate: 1. Install pool with docker. 2. Run CLI on any node and send some commands. 3. Install libindy to any node: {quote}apt-key adv --keyserver keyserver.ubuntu.com --recv-keys 68DB5E88 sudo add-apt-repository "deb https://repo.sovrin.org/sdk/deb xenial master" sudo apt update sudo apt-get install -y libindy{quote} 4. Clone indy-sdk repository to this node. 5. Run python wrapper acceptance test on installed pool: {quote}python3.5 -m pip install -e . python3.5 src/main.py{quote} 6. Check results.  Actual Results: Any node of the pool can be used as client (CLI) and libindy installed also works with this pool.  ></body> </Action>
