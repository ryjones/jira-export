<Issue id="21098" key="INDY-884" number="884" project="10303" reporter="krw910" assignee="krw910" creator="krw910" type="10004" summary="Unable to create a ledger from genesis files if using configuration override settings to point to the live genesis files" priority="1" resolution="10200" status="10001" created="2017-09-29 22:19:52.0" updated="2019-03-29 20:33:44.0" resolutiondate="2019-03-29 20:33:44.0" votes="0" watches="1" workflowId="21105"> <description><! CDATA *Build* indy-plenum= 1.1.27 indy-anoncreds= 1.0.10 indy-node= 1.1.37 sovrin= 1.1.6  1. Bring up a pool, but don't start the services yet. 2. From the sovrin user go into the .sovrin directory 3. Copy the sandbox files as live files {code} cp pool_transactions_sandbox_genesis pool_transactions_live_genesis cp domain_transactions_sandbox_genesis domain_transactions_live_genesis {code}  4. Edit the sovrin_config.py file and add the following two lines. These lines override the default and use the live transactions files to create the ledger under directories using 'live' in the naming {code} poolTransactionsFile = 'pool_transactions_live' domainTransactionsFile = 'transactions_live' {code}  5. Start the sovrin node service  {code} systemctl start sovrin-node {code}  *The ledgers should be created under the sovrin user in* {code} .sovrin/data/nodes/<node name>/pool_transactions_live_genesis .sovrin/data/nodes/<node name>/transactions_live_genesis {code}  *To view the ledgers run the tool read_ledger as the sovrin user with the following command* {code} read_ledger --type domain {code} *or* {code} read_ledger --type pool {code}  {color:#d04437}*Issue*{color} In either case nothing is returned. If I try a transaction using the default users from the domain_transactions_live_genesis file I get a message that the verkey of the DID cannot be found.  This happens using an install of the latest stable build (1.1.37). If you have upgraded from 1.0.28 to 1.1.37 you do not run into this issue due to a fix that occurred with the migration script for the upgrade process.        ></description> </Issue>
