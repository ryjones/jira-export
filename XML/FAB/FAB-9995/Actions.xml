<Action id="45343" issue="30104" author="senthil1" type="comment" created="2018-05-31 13:37:50.0" updateauthor="senthil1" updated="2018-05-31 13:37:50.0"> <body><! CDATA  https://gerrit.hyperledger.org/r/#/c/22583/   To run the test, do the following * If any changes had been made to local master branch, please commit those changes. * Install jq and screen ** ubuntu: apt-get install jq screen ** mac: brew install jq screen * cd examples/e2e_cli/manual_test && ./data_consistency_with_a_new_peer_test.sh    Scenario: # There are two organization: Org1 and Org2. One peer per organization and one orderer for the whole network. Instead of docker containers for peers/orderers, we use native binaries. # The marbles and marbles_private chaincodes are used throughout. # Start the network using fabric v1.0 executables. Install and instantiate marbles chaincode.  # Invoke and Query private data. # Upgrade the network to v1.1 without enabling without private data capability to ensure that collections definition are not allowed during chaincode instantiation # Invoke and Query public data # Enable private data capability to ensure that collections definition are allowed # Invoke and Query public data # Upgrade the network to v1.2 without enabling v12 capability to ensure that collection upgrades are not allowed during chaincode upgrade # Enable v12 capability to ensure that collections upgrades are allowed during chaincode upgrade  # Invoke and Query public data # Invoke and Query private data # Add a new peer in org2 cluster # Join the newly started peer to the channel and wait for the peer to fetch all missing blocks # On the new peer, query the public data stored while the network was in 1.0, 1.1, and 1.2 # On the new peer, query the private data stored while the network was in 1.0, 1.1, and 1.2   ></body> </Action>
<Action id="67529" issue="30104" author="sykesm" type="comment" body="Stale" created="2020-01-22 22:32:47.0" updateauthor="sykesm" updated="2020-01-22 22:32:47.0"/>
