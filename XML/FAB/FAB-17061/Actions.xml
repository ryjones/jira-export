<Action id="69449" issue="43300" author="wlahti" type="comment" created="2020-06-11 19:05:09.0" updateauthor="wlahti" updated="2020-07-09 11:55:05.0"> <body><! CDATA The link in the description is no longer available since it linked to gerrit. From looking at the code at that time, this seems to be some of the  code in question|https://github.com/hyperledger/fabric/blob/master/orderer/consensus/etcdraft/consenter.go  : {code:go} // when we have a system channel if c.InactiveChainRegistry != nil { return NewChain( support, opts, c.Communication, rpc, c.BCCSP, func() (BlockPuller, error) { return NewBlockPuller(support, c.Dialer, c.OrdererConfig.General.Cluster, c.BCCSP) }, func() { c.InactiveChainRegistry.TrackChain(support.ChannelID(), nil, func() { c.CreateChain(support.ChannelID()) }) }, nil, ) }  // when we do NOT have a system channel return NewChain( support, opts, c.Communication, rpc, c.BCCSP, func() (BlockPuller, error) { return NewBlockPuller(support, c.Dialer, c.OrdererConfig.General.Cluster, c.BCCSP) }, func() { c.Logger.Warning("Start a follower.Chain: not yet implemented") //TODO start follower.Chain }, nil, )  {code}    And  here|#diff-31c50bd6bdb92751de2354b5ac74099e  is the commit (without the gerrit reviews, of course).  ></body> </Action>
