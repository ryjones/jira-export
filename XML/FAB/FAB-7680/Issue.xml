<Issue id="26700" key="FAB-7680" number="7680" project="10002" reporter="scottz" assignee="sambhavdutt" creator="scottz" type="10004" summary="some repos are not updated in fabric-test/Makefile" priority="3" resolution="10000" status="6" created="2018-01-09 23:04:00.0" updated="2018-07-20 14:15:24.0" resolutiondate="2018-01-10 19:30:11.0" votes="0" watches="2" workflowId="40930"> <description><! CDATA In fabric-test/Makefile, similar to fabric and ca, we should also (optionally) clone and git pull the latest code in the other repositories which are submodules (because they could be referenced in some of our tests: cello, fabric-samples, fabric-sdk-node.  We do not need to build any objects in those repositories. That is, there is no need to add anything to docker-images.  We need something like the following, but for each of the other listed missing repos:  .PHONY: ca ca: if   ! -d "$(CA_DIR)"  ; then \ echo "Clone CA REPO"; \ cd $(HYPERLEDGER_DIR); \ git clone $(FABRIC_CA) $(CA_DIR); \ fi cd $(CA_DIR) && git pull $(FABRIC_CA)     ></description> </Issue>
