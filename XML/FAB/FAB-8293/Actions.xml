<Action id="41098" issue="27739" author="christopherferris" type="comment" created="2018-03-01 18:32:43.0" updateauthor="christopherferris" updated="2018-03-01 18:32:43.0"> <body><! CDATA #!/bin/sh set -e  export RELEASE=$\{1:-v1.1.0-rc1}  tag() \{   cd $GOPATH/src/github.com/hyperledger/$\{1}   git checkout master   git pull   COMMIT=`git log -n 1 --pretty=oneline | head -c7`   if  $1 === "fabric-samples" ; then                 git tag -a $\{RELEASE} $\{COMMIT} -m "Aligned with Hyperledger Fabric $\{RELEASE}"   else                 git tag -a $\{RELEASE} $\{COMMIT} -F release_notes/$\{RELEASE}.txt   fi   git push origin $\{RELEASE} HEAD:refs/heads/master }  # TODO add fabric-sdk-java # for x in fabric fabric-ca fabric-sdk-node fabric-samples fabric-chaincode-node; do for x in fabric fabric-ca fabric-samples; do   tag $x done ~                      ></body> </Action>
<Action id="41105" issue="27739" author="denyeart" type="comment" created="2018-03-01 19:22:58.0" updateauthor="denyeart" updated="2018-03-01 19:57:41.0"> <body><! CDATA {color:#ff0000}*NOTE:*  The script above cannot be used due to dependencies: {color} * {color:#ff0000}fabric dockerhub image must exist before fabric-ca will verify.{color} * {color:#ff0000}fabric-ca dockerhub image must exist before fabric-node-sdk will verify.{color}  {color:#ff0000}*CAUTION:* If using the above script to tag repositories, double-check the list of repositories, and ensure that each one's HEAD is sitting at the release CR, and not the "prepare for next" CR.{color}  ></body> </Action>
