<Issue id="15951" key="FAB-3358" number="3358" project="10002" reporter="mastersingh24" assignee="mastersingh24" creator="mastersingh24" type="10004" summary="Remove testchainid from peer start up" priority="1" resolution="10000" status="6" created="2017-04-23 17:37:39.0" updated="2018-07-20 14:12:21.0" resolutiondate="2017-05-29 18:40:13.0" votes="0" watches="1" timeoriginalestimate="10800" timeestimate="0" timespent="900" workflowId="38124"> <description><! CDATA I'm not sure if it's even used anymore other than in tests, but we must remove testchainid from the peer startup.  Minimally I'll settle for making the default false.  this code should be removed from the peer altogether IMHO:   {code} if peerDefaultChain { 		if orderingEndpoint == "" { 			logger.Panic("No ordering service endpoint provided, please use -o option.") 		}  		if len(strings.Split(orderingEndpoint, ":")) != 2 { 			logger.Panicf("Invalid format of ordering service endpoint, %s.", orderingEndpoint) 		}  		chainID := util.GetTestChainID()  		var block *cb.Block  		func() { 			defer func() { 				if err := recover(); err != nil { 					logger.Fatalf("Peer configured to start with the default test chain, but supporting configuration files did not match.  Please ensure that configtx.yaml contains the unmodified SampleSingleMSPSolo profile and that msp/sampleconfig is present.\n%s", err) 				} 			}()  			genConf := genesisconfig.Load(genesisconfig.SampleSingleMSPSoloProfile) 			genConf.Orderer.Addresses =   string{orderingEndpoint} 			genConf.Application.Organizations 0 .Name = XXXDefaultChannelMSPID 			genConf.Application.Organizations 0 .ID = XXXDefaultChannelMSPID 			block = provisional.New(genConf).GenesisBlockForChannel(chainID) 		}()  		//this creates testchainid and sets up gossip 		if err = peer.CreateChainFromBlock(block); err == nil { 			fmt.Printf("create chain  %s ", chainID) 			scc.DeploySysCCs(chainID) 			logger.Infof("Deployed system chaincodes on %s", chainID) 		} else { 			fmt.Printf("create default chain  %s  failed with %s", chainID, err) 		} 	} {code}   ></description> </Issue>
