<Action id="68554" issue="44648" author="sykesm" type="comment" created="2020-03-24 20:34:45.0" updateauthor="sykesm" updated="2020-03-24 20:34:45.0"> <body><! CDATA The test that hung up appears to be  {code} 		It("can remove leader by reconfiguring cluster", func() { 			network.elect(1)  			// trigger status dissemination 			Eventually(func() int { 				c1.clock.Increment(interval) 				return c2.fakeFields.fakeActiveNodes.SetCallCount() 			}, LongEventualTimeout).Should(Equal(2)) 			Expect(c2.fakeFields.fakeActiveNodes.SetArgsForCall(1)).To(Equal(float64(2)))  			By("Configuring cluster to remove node") 			Expect(c1.Configure(configEnv, 0)).To(Succeed()) 			Eventually(c2.support.WriteConfigBlockCallCount, LongEventualTimeout).Should(Equal(1)) 			c1.clock.WaitForNWatchersAndIncrement(ELECTION_TICK*interval, 2) {code}  Looks like the clock never had 2 watchers so it was never incremented.  ></body> </Action>
