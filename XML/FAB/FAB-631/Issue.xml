<Issue id="12933" key="FAB-631" number="631" project="10002" reporter="muralisr" assignee="muralisr" creator="muralisr" type="10001" summary="Pared down minimal Peer for getting fabric ready for new architecture work" priority="1" resolution="10000" status="6" created="2016-10-08 15:27:16.0" updated="2018-07-20 18:55:07.0" resolutiondate="2017-04-02 15:37:28.0" votes="0" watches="2" workflowId="36404"> <description><! CDATA Few things need to be done to clean up the fabric post skeletal end-to-end flow to get it ready for the community to do full-fledged new architecture work.   This can be divided into three categories * merge -  this can be done in three ways {quote}# merge `master` to `convergence` and then clean up # merge `convergence` to `master` and then clean up # clean up `convergence` and cherry pick fixes from `master` (see https://gerrit.hyperledger.org/r/#/c/1633 for a WIP on this approach){quote}  * clean up -  started list laundry list of sub-tasks (again, see https://gerrit.hyperledger.org/r/#/c/1633) {quote}* remove vestiges of `ledger` and use only `ledgernext` in chaincode framework * remove `consensus` from 'peer` executable * get chaincode unit tests to use ledgernext * remove encryption / decryption from chaincode{quote}  * use protobuf definition  {quote}* finalize protobuf definition (see https://jira.hyperledger.org/browse/FAB-489) * remove old protobuf definition (such as protos.Transaction) * use new protobuf definition{quote}  *{color:#d04437}The first thing to decide if we are going to merge or cherry pick.{color}*  ></description> </Issue>
