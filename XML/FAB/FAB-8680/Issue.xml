<Issue id="28252" key="FAB-8680" number="8680" project="10002" reporter="latitiah" creator="latitiah" type="10001" summary="Usability: The process for updating the endorsement policy for the chaincode is tedious" priority="3" resolution="10000" status="6" created="2018-03-06 19:18:56.0" updated="2018-07-20 18:50:19.0" resolutiondate="2018-04-23 16:03:19.0" votes="0" watches="3" workflowId="41453"> <description><! CDATA The process for updating the policy on a channel for a chaincode requires that one reinstalls the chaincode for that channel (even though the software necessarily does not change). One must install chaincode changing the version, even though it's the exact same chaincode as the previous version. So if one wants to change the endorsement policy from "OR" to "AND", the following must be done when using CLI commands: {code} $ peer chaincode install --name mycc --version 1 --channelID behavesystest --tls true --cafile tlsca.example.com-cert.pem --orderer orderer.example.com:7050 --path github.com/hyperledger/fabric/examples/chaincode/go/chaincode_example02 --lang GOLANG  $ peer chaincode upgrade --name mycc --version 1 --channelID behavesystest --tls true --cafile tlsca.example.com-cert.pem --orderer orderer.example.com:7050 --policy "AND ('org1.example.com.client','org2.example.com.client')" {code} Changing the policy seems similar to changing a software's configuration. Some organizations will change the version, others may not. A fresh deployment of the same software is not needed in either situation and should not be necessary here. There should be a way to reference an existing chaincode path when changing the policy. I can see the argument for both sides when deciding whether or not to force the incrementing of the version.  ></description> </Issue>
