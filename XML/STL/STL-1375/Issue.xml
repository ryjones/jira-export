<Issue id="32502" key="STL-1375" number="1375" project="10001" reporter="yoni" assignee="amundson" creator="yoni" type="10005" summary="Allow read several addresses once from TP by using address prefix" priority="3" status="10100" created="2018-08-02 11:28:41.0" updated="2019-05-21 14:34:00.0" votes="1" watches="5" workflowId="43719"> <description><! CDATA When posting read request to rest api there is an option to request address prefix and get list of addresses starting with this prefix, this doesn't require the user to know in advance which addresses exist.  would like to get the same option from TP, i.e. something like (depends on which language) map<address, data> = context,get_state(address_prefix);  Also, a lot of times not interested in addresses data, just to know which addresses contain data and which are empty. it would provide a big boost to performance (and allow use of more sophisticated TP logic for addresses) if had an option to read by prefix and and get list of addresses that contain data without the data itself     ></description> </Issue>
